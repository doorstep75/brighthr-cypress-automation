// setup-env.js
// Simple placeholder script to prevent install errors.
// In the future, you can extend this to copy .env.example -> .env, or set defaults.

console.log('Environment setup: no configuration needed for now ✅');
process.exit(0);
